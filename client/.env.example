# Where the Express server runs. Next.js proxies /api and /uploads to it,
# so the browser only talks to this app's own domain (keeps auth cookies first-party).
# Local: http://localhost:5000   Production (Vercel): https://<your-service>.onrender.com
API_ORIGIN=http://localhost:5000
